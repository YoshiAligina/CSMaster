public class April25 
{
    public static void main(String[] args)
    {
        // Sequential Search searchers the entire array sequentially, and returns the index. 
        // Binary Search is
        // Insertion sort: best: O(n) or worst: O(n^2) so the overall is O(n^2)
        // Selection sort has to be o(n^2)

        // Finding a num in an unsorted array: Sequential Search
        // Finding a num in an sorted array: BInary Search
        // Sorting an almost sorted array: Insertian Sort
        // Finding a num in a 99% sorted array: Sequential
        // Finding a num in an almost sorted(90%) array: Sort, Then Binary
     }
}
