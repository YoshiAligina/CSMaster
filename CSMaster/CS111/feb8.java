public class feb8
{
    public static void main(String[] args)
    {
        String s = "goodbye!";
        System.out.println("hello world!" + s);
         // The hello world! was not a string, so I made it one. And we only need one equal sign. Plus public class should be negative.

         /* 
          * 1.  No.
          * 2.  No.
          * 3.  No.
          * 4.  No.
          * 5.  Yes.
          */

          /*
           * 1.  (!a && b) || (a || !b)
           *  True when the values are NOT a AND IS b. OR when it is a OR NOT b.
           *  a & b = True
           *  !a & !b = True
           *  !a & b = True
           *  a & !b = True
           * ( a || b )|| (!a && !b) || (a && !b)
           *  True when answer is a OR b, OR NOT a and b OR a AND not B. 
           * a & b = True
           * !a & !b =  True
           * !a & b = True
           * a & !b = True
           */

           /*
            * (1+2+3+4)/ 4.0 = 1.0 DOUBLE
            * Math.sqrt(4) = 2.0 Double
            * "1234"+99 = 123499 INT
            * 11 * .25 = .275 DOUBLE
            * (int)11*.25  = .275 DOUBLE
            * 11 * (int).25 = 0 INT
            * int(11* .25) = 2 INT
            * int(2.71828) = 2 INT
            * Math.round((2.71828) = 3.0 DOUBLE
            * (int)Math.round((2.71828) = 3 INT
            * Integer.parseInt("1234") = 1234 INT

            */

            /*
             * 5 INT
             * 4.0 DOUBLE
             * 151 STRING
             * 245 STRING
             * ILLEGAL 
             * 6 INT
             * FALSE
             * FALSE
             */

             System.out.println(23/4);

        
    }
}